import { BlocoCarregando } from "@/components/CarregandoMarca";

/** Mostrado durante a navegação entre páginas da loja. */
export default function Carregando() {
  return <BlocoCarregando mensagem="Carregando" />;
}
